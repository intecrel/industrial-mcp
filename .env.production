# Industrial MCP - Production Environment Variables
# This file contains all environment variables needed for production deployment
# Upload these to your Vercel project settings

# =====================================================================
# AUTHENTICATION (REQUIRED)
# =====================================================================

# MAC address for hardware verification (REQUIRED)
# Format: XX:XX:XX:XX:XX:XX (case-insensitive)
MAC_ADDRESS=84:94:37:e4:24:88

# API key for securing MCP endpoints (REQUIRED)
# Use a long, random string. This is used for /api/mcp authentication
API_KEY=Yj8PpB3wWQHg5Z@f2LqS7xM9nV6tD4kJ

# Comma-separated list of IP addresses allowed to bypass MAC verification (OPTIONAL)
# Example: 127.0.0.1,203.0.113.42,2001:db8::1
ALLOWED_IPS=192.168.12.154

# =====================================================================
# NEO4J DATABASE CONNECTION (REQUIRED for graph functionality)
# =====================================================================

# Neo4j Aura (or self-hosted) connection URI (REQUIRED)
# Format: neo4j+s://xxxxx.databases.neo4j.io
NEO4J_URI=neo4j+s://1f4ca669.databases.neo4j.io

# Neo4j database username (REQUIRED)
NEO4J_USERNAME=neo4j

# Neo4j database password (REQUIRED)
NEO4J_PASSWORD=akM-Ss6PEGzsFSkUcrJFx2sRZDeLUwnO5NAgrlgcrvA

# Optional: Aura instance information
AURA_INSTANCEID=1f4ca669
AURA_INSTANCENAME=Instance01

# =====================================================================
# EXTERNAL SERVICES (OPTIONAL)
# =====================================================================

# Anthropic/Claude API key for AI integration (OPTIONAL)
# Only required if using Claude AI integration
ACCESS_TOKEN=your-anthropic-api-key-here

# =====================================================================
# DEPLOYMENT SETTINGS (OPTIONAL - auto-detected in most cases)
# =====================================================================

# Base URL for the application (OPTIONAL)
# This is automatically set in Vercel deployments via VERCEL_URL
# Only set this if you need to override the automatic detection
# BASE_URL=https://your-custom-domain.com

# =====================================================================
# IMPORTANT NOTES:
# - Do not use quotes around values
# - Do not add spaces around the equals sign
# - All REQUIRED variables must be set for the application to work
# - OPTIONAL variables can be left blank if not needed
# =====================================================================
